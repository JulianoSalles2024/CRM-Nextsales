{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/install/migrate.ts": {
      "includeFiles": "supabase/migrations/**"
    }
  }
}
